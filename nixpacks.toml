[phases.setup]
nixPkgs = ["python311", "git", "git-lfs"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python -m backend.model_download"]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
